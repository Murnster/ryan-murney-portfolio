{
	"name": "ryan-murney",
	"version": "1.0.0",
	"description": "Portfolio Website",
	"scripts": {
		"build": "node scripts/build.js",
		"dev": "npm run build && wrangler pages dev dist --compatibility-date=2026-05-03",
		"test": "echo \"Error: no test specified\" && exit 1",
		"deploy": "npm run build && wrangler pages deploy dist --project-name=ryan-murney"
	},
	"author": "Ryan Murney",
	"license": "ISC",
	"devDependencies": {
		"wrangler": "4.98.0"
	}
}
